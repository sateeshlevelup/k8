apiversion: v1 
kind: ReplicationController
metadata:  
 name: myapp 
 lable:
  app:myapp
  type:front-end
spec:
 template: 
    metadata:  
    name: myapp 
    lable:
      app:myapp
      type:front-end
    spec:
     containers: 
      - name : ubuntu
        image : ubuntu
